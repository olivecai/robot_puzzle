TODO
- shmalz gripper impl on off urscript gripper_api.py
- using the solution json and the detected json, make a plt and: for each piece, match the current to one in the solution key, and draw an arrow from that piece to the intended location, plus a rotation 
- 

DOCUMENTATION SO FAR:
- user just needs to set values in const.py 
- different classes exist for each component, the rule you jsut have to follow is the trait
- camera capture works but its b
- if you want gaps between puzzle pieces, do it in the solution